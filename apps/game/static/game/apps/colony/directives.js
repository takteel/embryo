'use strict';

/* Directives */

var ColonyDirectives = angular.module('Colony.directives', []);

/*
ColonyDirectives.directive('colonyVersion', [ 'version', function (version) {
	return function(scope, elm, attrs) {
		elm.text(version);
	};
}]);
*/
