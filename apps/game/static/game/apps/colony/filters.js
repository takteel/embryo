'use strict';

/* Filters */

var ColonyFilters = angular.module('Colony.filters', []);

/*
ColonyFilters.filter('interpolate', [ 'version', function (version) {
	return function(text) {
		return String(text).replace(/\%VERSION\%/mg, version);
	}
}]);
*/
